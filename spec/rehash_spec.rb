describe "`rehash` command" do

  context "when there are pending aliases" do

    it "creates shims for them" do
    end

    context "when the shim dir is in the user's path" do

      it "makes the alias available for use" do
      end
    end
  end
end
