# This file is autogenerated by the aliases tool.
# For more info about aliases type `aliases --help`
# or visit https://github.com/sebglazebrook/aliases

---
build:
  command: docker build -t aliases-dev-image .
shell:
  command: docker run -it -v ${PWD}:/code -w /code aliases-dev-image bash
release:
  command: "cargo clean && ./scripts/build-release-osx.sh $0 && ./scripts/build-release-debian.sh $0"
run-tests:
  command: "./scripts/test-runner.sh"
tester:
  command: "arg=$(FZF) && $0 $arg && echo $0 $arg >> ~/.bash_history"
